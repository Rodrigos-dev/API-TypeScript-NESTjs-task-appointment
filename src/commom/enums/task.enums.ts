export enum StatusTaskEnum {
    PENDING = 'pending',
    CANCELLED = 'cancelled',
    FINISHED = 'finished'
}

export enum PeriodTasksEnum {
    day = 'day',
    week = 'week',
    month = 'month',
    year = 'year'    
}